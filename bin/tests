#!/usr/bin/env node

require('child_process').exec('node_modules/mocha/bin/mocha tests/tests.js',
  function (error, stdout, sterr) {
    if (!sterr) {
      console.log(stdout);      
    } else {
      console.log(sterr);
    }
  });