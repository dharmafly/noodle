#!/bin/bash

# Start the testing server and store its pid
node tests/server.js & pid=$!

# Run the tests
node_modules/mocha/bin/mocha tests/tests.js --timeout 30000 --reporter spec

# Kill the pid of the test server
kill $!